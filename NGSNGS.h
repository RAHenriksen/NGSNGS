#ifndef NGSNGS_H
#define NGSNGS_H




void handler(int s);

void catchkill();

#endif

// -ll | --lengthlimit
// -fl | --fraglength -> So fragment length in regads to PE and then the current -l, -lf, -ld would be read lengths
